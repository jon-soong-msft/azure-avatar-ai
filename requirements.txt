# Core dependencies - keep minimal for small image size
azure-cognitiveservices-speech==1.40.0
azure-identity==1.15.0
flask==3.0.0
flask-socketio==5.3.5
openai==1.54.0
httpx==0.27.0
pytz==2023.3
python-dotenv==1.0.0
requests==2.31.0

# Production server
gunicorn==21.2.0
eventlet==0.33.3

# Lightweight numpy (no MKL)
numpy==1.26.2

# Note: PyTorch CPU-only will be installed in Dockerfile to keep size minimal
# Do not include torch with CUDA as it adds 4+ GB to the image
